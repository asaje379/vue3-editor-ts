const api = {
  // props: {
  //   customModules: Array,
  // },
  // methods: {
  //   registerCustomModules: function (Quill: {
  //     register: (arg0: string, arg1: any) => void;
  //   }) {
  //     if (this.customModules !== undefined) {
  //       this.customModules.forEach(
  //         (customModule: { alias: string; module: any }) => {
  //           Quill.register(
  //             'modules/' + customModule.alias,
  //             customModule.module,
  //           );
  //         },
  //       );
  //     }
  //   },
  // },
};

export default api;
